# 2026-03-14

## Deleted-agent leftovers identified in ~/.openclaw
- During cleanup review, `openclaw status` reported 31 agents / 31 stores, but filesystem inspection showed likely orphaned leftovers from deleted agents.
- Confirmed likely orphaned paths:
  - `~/.openclaw/agents/ada`
  - `~/.openclaw/memory/ada.sqlite`
  - `~/.openclaw/memory/writer.sqlite`
- Reasoning: `ada` and `writer` did not appear in the visible active agent/status set, but stale runtime/memory artefacts remained on disk.
- Recommended smallest-safe cleanup first: remove only those three paths, then re-run status and list `~/.openclaw/agents` + `~/.openclaw/memory` to verify.

## Approval flow issue observed
- Repeated exec approvals failed with `GatewayClientRequestError: unknown or expired approval id` even after issuing fresh approval tokens.
- Practical workaround used: ask Aaron to run the inspection commands directly in the TUI shell and paste output for analysis.
